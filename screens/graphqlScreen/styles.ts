import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingTop: 15,
    backgroundColor: '#fff',
  },
  button: {
    height: 50,
    width: 200,
    borderWidth: 1,
    borderColor: 'black',
    alignContent: 'center',
    margin: 10,
    padding: 10,
  },
});
